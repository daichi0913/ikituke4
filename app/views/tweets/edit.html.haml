.contents.row
  = form_tag("/tweets/#{@tweet.id}", method: :patch ) do
    %h2
      編集する
    %textarea{:cols => "30", :name => "text", :placeholder => "text", :rows => "10"}= @tweet.text
    %br/
    %input{:type => "submit", :value => "変更する"}/
